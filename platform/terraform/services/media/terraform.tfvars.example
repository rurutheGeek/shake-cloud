# platform/terraform/services/media の入力例。
# **terraform.tfvars はコミットしない。** state と同様、秘密値が入り得る。
#
#   cp terraform.tfvars.example terraform.tfvars
#   # 必要な値だけ書く

# cloud API の鍵ペアへ登録する公開鍵のファイル。秘密鍵ではない。
# 正本は platform/terraform/access.yaml の admin_ssh_public_keys。
# 既定は ~/.ssh/id_ed25519.pub。
# ssh_public_key_path = "~/.ssh/id_ed25519.pub"

# サイズは I01 の実測が確定するまでの開始予算（4vCPU／6GiB、OS32＋データ64GiB）。
# vcpus          = 4
# memory_mib     = 6144
# memory_min_mib = 2048
# root_disk_gib  = 32
# data_disk_gib  = 64
