# Ansible が配備ごとに描画する宣言値。秘密値はここへ書かない。
# IMAP_USERNAME は role が SOPS から、IMAP_PASSWORD は manage.py が
# secrets/imap_password から環境変数として渡す。
MAIL_VIEW_PORT={{ mail_view_port }}
IMAP_HOST={{ mail_view_imap_host }}
IMAP_PORT={{ mail_view_imap_port }}
MAILBOX={{ mail_view_mailbox }}
MESSAGE_LIMIT={{ mail_view_message_limit }}
CACHE_SECONDS={{ mail_view_cache_seconds }}
TZ={{ mail_view_tz }}
