# Ansible が配備ごとに描画する宣言値。秘密値はここへ書かない。
# 統計ページのパスワードは manage.py が secrets/stats_password から
# 環境変数として渡す。
STORAGE_ROOT={{ librespeed_storage_root }}
LIBRESPEED_PORT={{ librespeed_port }}
TZ={{ librespeed_tz }}
