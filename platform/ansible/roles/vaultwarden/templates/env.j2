# Ansible が配備ごとに描画する宣言値。秘密値はここへ書かない。
# SSO のクライアント秘密値は manage.py が secrets/oidc_client.json から
# 環境変数として渡す。
STORAGE_ROOT={{ vaultwarden_storage_root }}
VAULTWARDEN_PORT={{ vaultwarden_port }}
VAULTWARDEN_SIGNUPS_ALLOWED={{ vaultwarden_signups_allowed }}
VAULTWARDEN_INVITATIONS_ALLOWED={{ vaultwarden_invitations_allowed }}
TZ={{ vaultwarden_tz }}
