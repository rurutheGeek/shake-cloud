# Copy to netbox.env, chmod 600, then: set -a; . ansible/netbox.env; set +a
NETBOX_API='https://netbox.example.com'
NETBOX_TOKEN='REPLACE_WITH_READ_ONLY_API_TOKEN'
# v2 tokens: nbt_<key>.<token>. For a legacy v1 token only, use Token.
NETBOX_AUTH_TYPE='Bearer'
