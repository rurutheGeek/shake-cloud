# Nextcloudの「印刷」アクション（shake_print）と services-01 の印刷APIが
# 共有するトークン。正本はSOPS（print-api.sops.yaml）。
#
# 生成: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
PRINT_API_TOKEN: REPLACE_WITH_A_RANDOM_TOKEN
