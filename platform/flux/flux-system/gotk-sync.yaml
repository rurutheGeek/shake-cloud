# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/rurutheGeek/shake-cloud
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./platform/flux
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # 秘密値は SOPS で暗号化して置く。age 鍵はクラスタ内の Secret にある
  # （bootstrap では作られないので、配備時に別途入れる）。
  decryption:
    provider: sops
    secretRef:
      name: sops-age
