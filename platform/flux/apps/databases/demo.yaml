# 最初の PostgreSQL。まずはプライマリ1台。データは local-path（worker-01 のデータディスク）。
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo
  namespace: databases
spec:
  instances: 1
  storage:
    storageClass: local-path
    size: 5Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
