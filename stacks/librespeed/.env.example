# LibreSpeed スタックの宣言値。秘密値は secrets/ に置き、ここへ書かない。
#
# 統計ページ（/results/stats.php）のパスワードは manage.py が
# secrets/stats_password から環境変数で Compose へ渡す。
STORAGE_ROOT=/srv/services/librespeed
LIBRESPEED_PORT=8300
TZ=Asia/Tokyo
