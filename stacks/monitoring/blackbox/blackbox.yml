# blackbox_exporter の probe 定義。
modules:
  https_2xx:
    prober: http
    timeout: 10s
    http:
      preferred_ip_protocol: ip4
      valid_http_versions: [HTTP/1.1, HTTP/2.0]
      fail_if_not_ssl: true
  http_2xx:
    prober: http
    timeout: 10s
    http:
      preferred_ip_protocol: ip4
