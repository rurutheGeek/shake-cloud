# D01 開発参加ガイドの更新

更新日: 2026-09-12。種別: **資料のみ更新**。状態: **資料更新・文書検証完了（実機作業の完了とは別）**。

[開発計画一覧](index.md)へ戻る。番号は実施順を表さない。

## 目的・現状の根拠

新規VM・各機能の担当者が、独立した作業ディレクトリと計画書へ到達できる案内にする。

[開発参加ガイド](../onboarding.md)にdev-a／dev-b、SSH、起動停止の既存手順がある。[クラウドの利用案内](../services/cloud.md)と[Provider手順](../operations/terraform-provider.md)は既に存在する。APIによる払い出し全体を未実装と表記しない。

## 変更範囲

開発参加ガイドの「今後」を現在のAPI・Providerと未完了の配備連携に分け、[計画一覧](index.md)から担当IDを選ぶ導線を追加する。作業用dev-a／dev-bとサービスを配備するVMを区別し、別作業ディレクトリで並列開発する。既存のログイン情報や過去の確認日は証拠なく更新しない。

配備先: 文書は本リポジトリの `docs/`。公開サイトへの反映は文書検証後の既存運用に従い、この計画整備では公開しない。

## 依存関係・並列作業

開始: なし。配備・切替: 文書のみなのでなし。I03・I04・I05が完了した時点で該当案内のみ追記する。

共有変更: `docs/onboarding.md`、`docs/services/devvm.md`、`mkdocs.yml` は D02・D04 と変更範囲を共有する。

## 検証・完了条件

案内のリンクを辿ってSSH・起動停止・担当計画・サービスVM手順へ到達できる。APIの存在と未完了項目を区別できる。文書ビルドと公開対象チェックの通過後に資料更新を完了扱いとする。

共通の確認は `python3 -m mkdocs build --strict`、内部リンクの実在確認、`python3 tools/check-publication.py`。実施していない実機確認を完了根拠へ追加しない。

検証記録（2026-09-12）: 厳格ビルド、ローカルリンク・ID対応、公開対象チェック、差分の空白検査が通過。実機確認はこの資料更新では実施していない。
