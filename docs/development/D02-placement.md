# D02 配置・配分表・構成図の更新

更新日: 2026-09-12。種別: **資料のみ更新**。状態: **進行中（配置表・原稿を更新。SVGの文字幅を含む最終描画は未完了）**。

[開発計画一覧](index.md)へ戻る。番号は実施順を表さない。

## 目的・現状の根拠

今回決定した機能別の置き場所、初期予算、同居停止の影響を資料間で揃える。

[配分と運用設計](../architecture/operations.md)、[ゲーム構成](../architecture/gaming.md)、[配備台帳](../operations/handover.md)には設計値と実測記録がある。2026-09-10の空き容量を現在値として扱わない。

## 変更範囲

services-01を4vCPU／8GiB候補、game1を8vCPU／現行12GiB・16GiB候補、media-01を4vCPU／6GiB・OS32＋データ64GiB仮予算とする。HA Container・VPN・Homarr・Vaultwardenはservices-01、AI一式はgame1へ揃える。public-edgeは1vCPU／1GiB・OS16GiBの後続候補。既存KubernetesはAWX・DB・関数の配置を保持する。全体表の合計を再計算し、旧実測を日付付きで残す。構成図を変更したらMermaid原稿とSVGを同じ内容にする。

配備先: 文書は本リポジトリの `docs/`。公開サイトへの反映は文書検証後の既存運用に従い、この計画整備では公開しない。

## 依存関係・並列作業

開始: なし。配備・切替: 文書のみなのでなし。[I01](I01-resources.md)の実測結果は取得後に追記し、[I02](I02-media-vm.md)のVM作成完了と混同しない。

共有変更: `docs/architecture/` とその図、配備台帳、計画一覧は D03・D04・I01 と調整する。図の原稿とSVGを別担当が同時編集しない。

## 検証・完了条件

配置表と図のVM名・用途・合計が一致し、計画と実機を区別できる。AI専用VMとHomarr／VaultwardenのKubernetes移行案が現行方針に残っていない。文書・リンク・公開対象検証を通す。

共通の確認は `python3 -m mkdocs build --strict`、内部リンクの実在確認、`python3 tools/check-publication.py`。実施していない実機確認を完了根拠へ追加しない。

検証記録（2026-09-12）: 厳格ビルド・リンク・公開対象チェックは通過。Mermaid原稿とSVGのラベルは照合済み。ただし初回描画環境にフォントがなく、SVGの文字幅・配置に問題があるため、表示用SVGの完了条件は未達。フォントを指定した再描画の実行許可が拒否され、再実行していない。I01・I02の並行作業による実測・VM作成結果は各計画書の記録を参照する。
