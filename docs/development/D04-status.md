# D04 実装状況の訂正と未完了一覧の整備

更新日: 2026-09-12。種別: **資料のみ更新**。状態: **資料更新・文書検証完了（実機作業の完了とは別）**。

[開発計画一覧](index.md)へ戻る。番号は実施順を表さない。

## 目的・現状の根拠

既存実装、既存環境からの移行、未実装、実機未確認、資料のみの作業を分け、重複開発を防ぐ。

[配備台帳](../operations/handover.md)、[クラウド運用](../operations/cloud.md)、[サービスの作り方](../operations/services.md)、[identity運用](../operations/identity.md)が根拠。API／Provider、メディアCompose、identityの設定コードは既にあり、新配置への移行完了とは別。

## 変更範囲

構成案の未完了項目を[ID一覧](index.md)へ対応付ける。クラウドAPIを将来の未実装とする記述を訂正し、AnsibleのクラウドVM連携・DNS自動登録・サービス用state・外部バックアップは各I／O計画へ委ねる。既存SSOを再実装せず移行時の再接続として記載する。資料・コードの存在と稼働中の確認を別々に日付付きで記録する。

配備先: 文書は本リポジトリの `docs/`。公開サイトへの反映は文書検証後の既存運用に従い、この計画整備では公開しない。

## 依存関係・並列作業

開始: なし。配備・切替: 文書のみなのでなし。各IDの実装完了報告が出たとき、その証拠を該当計画に追記する。

共有変更: 各計画書の状態、`docs/operations/handover.md`、構成案、オンボーディングは各担当・D01〜D03と照合する。仕様・進捗の正本は個別計画書とし一覧へ重複コピーしない。

## 検証・完了条件

対象構成案の未完了項目がIDまたは条件待ちに対応する。既存コードに対応した根拠があり、今回の資料更新をサービス配備済みと表示しない。文書・リンク・公開対象検証の結果を残す。

共通の確認は `python3 -m mkdocs build --strict`、内部リンクの実在確認、`python3 tools/check-publication.py`。実施していない実機確認を完了根拠へ追加しない。

検証記録（2026-09-12）: 厳格ビルド、ローカルリンク・ID対応、公開対象チェック、差分の空白検査が通過。実機確認はこの資料更新では実施していない。
